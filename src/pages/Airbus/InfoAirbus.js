import React, { Fragment } from "react";
import {Formulario} from "../../components/Formulario"

const InfoAirbus = () => (
  <Fragment>
    
    <h1>
      Hola Bienvenido, sabemos que quieres viajar en un Airbus por favor
      diligencia el siguiente formulario
    </h1>
    <Formulario/>
  </Fragment>
);

export default InfoAirbus;
